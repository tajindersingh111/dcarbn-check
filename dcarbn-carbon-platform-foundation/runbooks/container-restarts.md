# Repeated Container Restarts

1. Inspect exit codes, OOM events, health checks, and startup logs.
2. Confirm secret mounts, configuration validation, migrations, and dependency readiness.
3. Roll back when restarts began with a release.
4. Do not disable health checks to hide a failing service.
5. Confirm stable uptime and successful smoke tests for 30 minutes.
